package com.beeny.villagesreborn.core.combat;

/**
 * Enum representing different equipment slots for combat items
 */
public enum EquipmentSlot {
    MAINHAND,
    MAIN_HAND,  // Alias for compatibility
    OFFHAND,
    OFF_HAND,   // Alias for compatibility
    HEAD,
    CHEST,
    LEGS,
    FEET
}